name: tavle
version: "0.1"
profile: tavle
model-paths: ["models"]
test-paths: ["tests"]
target-path: target
clean-targets: [target, dbt_packages]
vars:
  raw_path: "../data/raw"
models:
  tavle:
    staging:
      +materialized: view
    marts:
      +materialized: table
